
minFreq = 100;
maxFreq = 8000;
bins = 12;
fs = 44100;

% L = size(audio_frames,2); % num audio frames 
% s = zeros([N,L]);
% cplx = zeros([N,L]);
s=[];
for i=1:size(frames,2)
      frame = frames(:,i);      
      cq = slowCQ(frame', minFreq, maxFreq, bins, fs);
      s(:,i) = (abs(cq).^2);       
end



figure; 
subplot(
plot(s(:,500)); hold on
plot(s(:,1000)); hold on

